# 家庭私有菜谱库 · 产品需求文档（PRD）

版本：v0.1  
日期：2026-09-09  
产品名称：老闫家菜谱  
状态：需求评审稿，尚未进入实现阶段

## 1. 产品定位与目标

面向家人内部使用的微信小程序，提供预置菜谱浏览、家庭菜谱管理和买菜清单。特色功能是粘贴外部菜谱链接，由后端获取内容、大模型整理并填写菜谱表单，经人工确认后保存入库。

界面采用正常字号，风格简洁、美观，操作流畅；不做专门的适老化设计。

产品目标：

- 打开即可浏览一批预置菜谱，不需要用户先录入内容。
- 将散落在外部网站的菜谱整理到家庭自己的菜谱库中。
- 减少手动填写菜谱的工作，同时保留人工审核和修改权。
- 将选中的菜谱食材转为可勾选的买菜清单。
- 通过家庭访问控制，使数据仅对获准成员开放。

开发学习目标：在项目中逐步学习微信小程序、Python 后端、数据库与大模型调用。第一版采用固定的 AI 提取流程，不以自主 Agent 为实现前提。

## 2. 需求状态说明

本文件将已确认需求与实施建议区分。标注“建议”的规则用于补全产品行为，尚不代表用户已逐项确认；标注“待确认”的内容在相应功能实施前确定。

已确认的核心方向：

- 微信小程序，仅供家人内部使用。
- 预先提供菜谱库，初始菜谱需要寻找合适的网上来源。
- 基础浏览、买菜清单、手动录入及编辑。
- 外部链接导入优先支持下厨房。
- 用户主动点击按钮发起 AI 解析，人工确认后保存。
- 正常字号，不做专门适老化设计。

## 3. 用户与使用场景

| 用户 | 主要使用场景 |
| --- | --- |
| 家庭成员 | 搜索菜谱、查看做法、添加买菜食材、勾选已买 |
| 维护菜谱的家庭成员 | 导入外部菜谱、添加家庭做法、检查 AI 草稿并保存 |
| 家庭管理员（建议角色） | 维护获准访问的成员名单 |

典型场景：

1. 准备做饭时，根据菜名或家中食材搜索菜谱，查看材料和步骤。
2. 在下厨房发现想做的菜，复制链接到小程序，解析并修改草稿后保存。
3. 将几道菜所需食材加入买菜清单，补充其他物品，采购时逐项勾选。
4. 将家人的拿手菜手动录入，记录与外部菜谱不同的家庭做法。

## 4. 第一版范围

| 模块 | 第一版要求 | 优先级 |
| --- | --- | --- |
| 预置菜谱 | 提供经过整理和检查的初始菜谱 | P0 |
| 菜谱浏览 | 列表、分类、菜名与食材搜索、详情 | P0 |
| 菜谱维护 | 手动新增、编辑、家庭备注 | P0 |
| AI 导入 | 下厨房链接优先适配、解析状态、可编辑草稿、确认保存 | P0 |
| 导入备用方式 | 获取链接失败后可粘贴正文进行解析 | P0 |
| 买菜清单 | 选择食材加入、手动补充、删除、已买勾选 | P0 |
| 家庭访问 | 验证成员身份，阻止未获准用户访问家庭数据 | P0 |

第一版不包含：公开社区、陌生人分享、支付或商城、营养诊断、自动膳食规划、视频菜谱解析、覆盖所有菜谱网站、自动发布未经确认的菜谱、多家庭运营平台。

## 5. 信息架构与页面

建议设置“菜谱”“买菜清单”“我的”三个主入口。

| 页面 | 内容与主要操作 |
| --- | --- |
| 菜谱首页 | 搜索框、分类、菜谱列表、“＋ 添加菜谱”按钮 |
| 菜谱详情 | 菜名、可选图片、食材及用量、步骤、补充信息、家庭备注、来源；编辑、加入买菜清单 |
| 添加方式选择 | “AI 链接导入”和“手动填写” |
| AI 导入页 | 链接输入框、“开始解析”；失败时提供正文输入方式 |
| 菜谱编辑／确认页 | 可编辑的完整表单、缺失提示、“确认保存”或“保存修改” |
| 买菜清单 | 待买和已买条目、数量与单位、手动添加、删除和勾选 |
| 我的 | 当前成员信息；成员管理入口是否提供取决于权限方案 |
| 无访问权限页 | 清楚说明尚未获得家庭访问权限 |

设计要求：正常字号、清晰的视觉层级、统一按钮样式。无图片的菜谱使用统一占位样式。AI 解析状态、失败状态和保存结果必须能被明确识别。

## 6. 功能需求

### 6.1 预置菜谱

- 在首批数据准备完成后，用户首次使用即可浏览菜谱。
- 从适合复用的菜谱来源筛选内容，统一整理为本产品的字段格式。
- 入库前检查食材、用量、步骤是否完整，保留来源及相关使用条件记录。
- 建议首批提供约 30 道常见家常菜，具体数量及菜品清单待确认。
- 图片不是必填项；来源和使用条件不明确时不作为预置图片使用。
- 预置菜谱与手动录入、链接导入菜谱使用相同的数据结构。

候选来源：HowToCook 开源菜谱项目。来源选择和具体内容尚未最终确定，不能将候选视为已完成的数据准备。

### 6.2 浏览、搜索与详情

- 展示已正式入库的菜谱；未确认的 AI 草稿不出现在列表中。
- 支持按分类筛选，以及按菜名或食材关键词搜索。
- 详情页按食材、步骤、家庭备注等内容分区展示。
- 份量、耗时和注意事项在有数据时展示，不虚构缺失信息。
- 无结果时展示空状态，并提供调整关键词或添加菜谱的入口。
- 建议提供“全部”分类；具体分类集合与默认排序待确认。

### 6.3 手动添加与编辑

- 手动添加和 AI 确认使用一致的菜谱表单。
- 支持编辑菜名、分类、食材、用量、步骤、补充信息和家庭备注。
- 建议正式保存至少要求：非空菜名、一项非空食材、一个非空步骤。
- 原文未提供的用量可以为“未注明”，不强制填写数字。
- 保存前检查必填项并定位问题，失败时保留已填写内容。
- 编辑权限及菜谱删除能力待确认；本版不默认开放任意成员删除菜谱。

### 6.4 AI 链接导入

入口：菜谱首页“＋ 添加菜谱” → “AI 链接导入”。

主流程：

1. 用户粘贴下厨房菜谱链接。
2. 用户点击“开始解析”，系统验证链接格式及支持范围。
3. 后端获取菜谱原始内容。
4. 大模型提取并填写统一格式的菜谱草稿。
5. 系统检查输出结构，展示可编辑表单及缺失信息。
6. 用户修改、补充并点击“确认保存”。
7. 后端验证权限和字段，将菜谱正式入库，展示保存结果。

提取字段：菜名、分类、简介、食材与用量、步骤、份量、耗时、注意事项、来源链接。分类可以使用“未分类”；原文未注明的事实字段保持缺失或标为“未注明”。来源链接由系统保留，不依赖模型编造。

关键规则：

- 点击“开始解析”后才发起导入所需的 AI 调用。普通浏览和清单操作不调用模型。
- 模型必须基于获取的正文提取，不能仅凭链接生成菜谱。
- 明确区分“获取网页内容”和“AI 整理内容”两个环节。
- 只有人工点击“确认保存”后才建立正式菜谱记录。
- 建议解析期间禁止重复提交，同一次确认保存避免生成重复记录。
- 建议识别到相同来源链接已入库时提示用户查看已有菜谱，再决定是否另存。
- 建议离开未保存的编辑页时提示确认；第一版不要求跨设备恢复草稿。

导入状态：等待输入 → 获取内容中 → AI 整理中 → 待确认 → 保存中 → 已保存。获取、解析或保存失败时显示相应错误及可执行的下一步。

| 情况 | 预期处理 |
| --- | --- |
| 链接为空或格式错误 | 不发起解析，提示修改链接 |
| 不支持的来源 | 说明当前支持范围，提供粘贴正文入口 |
| 登录限制、页面失效或获取失败 | 不生成假菜谱，允许粘贴正文 |
| 正文没有可识别菜谱 | 提示补充菜谱正文或手动填写 |
| 模型超时或服务不可用 | 提供重试，保留用户输入 |
| 模型输出结构不合格 | 不入库，提示重试或手动填写 |
| 部分字段缺失 | 标记缺失项，用户补充后保存；可选项可留空 |
| 保存失败 | 保留草稿，允许重试 |

下厨房接口状态：尚未确认可供本项目使用的官方开放 API。需先验证真实链接的获取方式、支持条件和稳定性；不承诺所有下厨房链接均可导入。遇到访问限制不绕过登录或访问控制。

### 6.5 买菜清单

- 从菜谱详情选择需要购买的食材后加入清单。
- 支持手动补充条目、删除条目、勾选已买及取消勾选。
- 同一食材、相同单位、明确数字用量可以相加，例如“鸡蛋 2 个”和“鸡蛋 3 个”合并为“鸡蛋 5 个”。
- “适量”、范围用量和不同单位不自动强行换算，保留原始说明或分列。
- 建议首版仅按规范化后的相同名称和单位合并，不自动推断“番茄”和“西红柿”为同一项。
- 建议加入后保存食材快照；随后编辑菜谱，不静默改变已有买菜清单。
- 买菜清单是全家共用还是成员各自独立，待确认；不把其中任一种当作已确定方案。

### 6.6 家庭访问与权限

- 所有菜谱和清单数据接口均由后端校验身份与权限。
- 仅在前端隐藏入口不能作为访问控制。
- 非成员不得读取、修改家庭数据，也不得使用家庭的 AI 导入接口。
- 家庭成员共享同一菜谱库。
- 成员加入方式、管理员设置、编辑范围和删除权限待确认。
- 开发阶段可评估用白名单管理少量成员；这是建议方案，尚未选定。
- 个人主体账号不自动等同于私有访问，仍需要应用层权限控制。

## 7. 数据结构（概念设计）

| 对象 | 主要字段 |
| --- | --- |
| 菜谱 | ID、菜名、分类、简介、可选图片、份量、耗时、注意事项、家庭备注、来源类型、来源链接、创建人、创建时间、更新时间 |
| 食材 | 所属菜谱、名称、原始用量文本、可选数字数量、可选单位、顺序 |
| 步骤 | 所属菜谱、序号、步骤正文 |
| 导入草稿 | 导入人、链接或正文、提取结果、缺失项、解析状态；保存方式及有效期待实现设计 |
| 买菜条目 | 食材名、用量文本、可选数字与单位、已买状态、来源菜谱、创建人；归属取决于共享方案 |
| 家庭成员 | 身份标识、显示名、角色、访问状态 |

数据要求：保留原始用量文本，避免因数字化转换丢失“少许”“半碗”等信息。正式菜谱与待确认草稿必须在逻辑上分离。

## 8. 技术边界与运行要求

- 小程序前端：使用微信开发者工具进行开发、调试和真机预览，初步采用原生小程序技术。
- 后端：初步采用 Python，负责内容获取、模型调用、字段校验、数据读写和权限检查。
- 模型：由后端调用外部大模型 API；不是将模型运行在小程序内，也不需要训练自有模型。
- 数据库、模型服务商、部署平台和正式发布方式待选型。
- 模型密钥及服务凭据只存放在后端配置中，不写入前端或公开仓库。
- 链接获取只允许经过验证的来源，校验重定向目标，阻止访问本地或内网地址，并限制请求时长和内容大小。
- 网页正文作为待提取数据处理，不允许其中的指令改变系统行为或触发无关工具。
- 解析请求应有超时、重复提交控制和基本调用次数限制；具体参数待验证后确定。
- 日志避免记录密钥和不必要的个人信息；上线前确定数据备份方式。
- 家庭试用与正式发布所需的平台条件，在部署阶段核实，不把体验版视为已确定的长期运行方案。

## 9. 第一版验收标准

| 编号 | 验收场景 | 通过标准 |
| --- | --- | --- |
| A01 | 首次进入菜谱库 | 可浏览事先准备并检查过的菜谱，详情可正常打开 |
| A02 | 搜索与分类 | 可用菜名或食材找到匹配菜谱；无结果时有明确提示 |
| A03 | 手动保存 | 合格表单可保存并出现在列表，缺少必填内容时提示修改 |
| A04 | 主动调用 AI | 输入链接但未点击开始解析时，不发起模型调用 |
| A05 | 链接成功导入 | 经确认支持的样例链接能够生成包含食材和步骤的可编辑草稿 |
| A06 | 缺失与真实性 | 原文缺失的用量不被自动编造，页面标记缺失信息 |
| A07 | 人工确认 | 未确认草稿不出现在正式库；确认后保存且重新打开仍可见 |
| A08 | 导入失败 | 获取或模型失败有原因提示与备用操作，不保存伪造结果 |
| A09 | 正文备用导入 | 粘贴完整菜谱正文可进入相同的解析、编辑、确认流程 |
| A10 | 买菜清单 | 可从菜谱加入食材、手动补充、删除、勾选和取消已买 |
| A11 | 用量合并 | 同名同单位数字正确相加；适量或不兼容单位不错误相加 |
| A12 | 家庭权限 | 非成员直接请求接口也无法读取、修改家庭数据或发起 AI 导入 |
| A13 | 普通使用 | 浏览、搜索、勾选买菜清单不会调用大模型 |
| A14 | 界面与失败恢复 | 正常字号、主要操作清楚；保存失败保留输入，可重试 |

下厨房导入验收以预先确认的真实样例集为准，样例集应包含成功页面、失效页面与受限页面。具体样例和稳定性目标在可行性验证后补充。

## 10. 建议实施顺序

1. **验证导入可行性**：用真实下厨房链接测试正文获取，再验证 AI 结构化提取，记录支持与失败条件。
2. **建立基础菜谱库**：确定初始来源，整理首批数据，完成列表、搜索、详情和手动维护。
3. **完成 AI 导入闭环**：输入、状态展示、可编辑草稿、人工确认、保存与失败备用入口。
4. **完成买菜清单和家庭权限**：按确认的共享及权限规则实现，进行端到端验证。
5. **家庭试用**：使用真实设备和实际菜谱检查操作体验、导入质量和清单行为，再处理部署事项。

以上为建议顺序，不构成交付工期承诺。

## 11. 待确认事项

| 事项 | 当前状态／建议 |
| --- | --- |
| 正式名称 | 已确认：“老闫家菜谱” |
| 初始菜谱来源及数量 | 尚未准备；建议先筛选约 30 道家常菜 |
| 下厨房获取方案 | 优先验证官方接口可用性和公开网页获取可行性 |
| 模型服务商与费用范围 | 待选型 |
| 菜谱分类与默认排序 | 待确定 |
| 家庭成员加入方式 | 待确定，可评估小规模白名单 |
| 菜谱编辑和删除权限 | 待确定；删除不默认开放 |
| 买菜清单归属 | 全家共用或个人独立，待确定 |
| 图片处理 | 可选；来源、存储方式与使用条件待确定 |
| 草稿保留方式 | 第一版不要求跨设备恢复，是否需要持久草稿待确定 |
| 部署与发布 | 待核实微信平台条件及运行成本 |

## 12. 参考资料

- [微信开发者工具官方下载入口](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
- [HowToCook 候选菜谱来源](https://github.com/Anduin2017/HowToCook)
- [HowToCook 仓库许可证](https://github.com/Anduin2017/HowToCook/blob/master/LICENSE)
- [下厨房社区指导原则](https://www.xiachufang.com/principle/)

以上链接用于后续实施核验，不代表已取得下厨房 API 接入能力或完成具体菜谱素材审查。
